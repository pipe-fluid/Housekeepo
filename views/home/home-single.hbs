Here we should display home info.


<a href="/home/{{home._id}}/edit">Delete or edit information about your home</a>
<a href="/tasks/{{home._id}}/create">Create a Task for your home</a>

{{home.name}}
{{#each home.members}}
{{name}}
{{/each}}
{{home.address}}
{{home.zipcode}}
{{home.phone}}

{{#each tasks}}
<a href="/tasks/{{_id}}"><h4>
  {{title}}</h4>
  <p>Urgency: {{urgency}}</p>
  <p>price: {{price.amount}} {{price.currency}}</p>
</a>
{{/each}}

<form action="/home/invite/{{home._id}}" method="post">
  <label for="email">Invite someone to this house: </label>
  <input name="email" type="email" placeholder="Email here" />
  <button class="btn btn-outline-primary" type="submit">Invite!</button>
</form>